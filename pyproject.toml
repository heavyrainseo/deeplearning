[project]
name = "deeplearning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Prof. Seo", email = "hvyrain@ysc.ac.kr" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "seaborn>=0.13.2",
    "tensorflow>=2.22.0rc0",
]

[project.scripts]
deeplearning = "deeplearning:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
