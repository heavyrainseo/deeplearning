# MNIST 분류 모델 설명서

이 문서는 초보자를 위해 작성된 `SM-mnist.ipynb` 코드에 대한 가이드입니다. 

---

## 1. 개요: MNIST란?
MNIST는 손글씨 숫자(0~9) 이미지 데이터셋입니다. 우리는 컴퓨터에게 이 이미지들을 보여주고, 어떤 숫자인지 맞추도록 학습시킬 것입니다.

## 2. 코드 단계별 상세 설명

### 1단계: 데이터 준비 및 확인
- **데이터 로드**: `tf.keras.datasets.mnist`를 사용하여 이미 학습된 60,000개의 훈련용 데이터와 10,000개의 테스트용 데이터를 가져옵니다.
- **정규화(Normalization)**: `x_train / 255.0` 코드는 픽셀 값(0~255)을 0과 1 사이의 값으로 변환합니다. 컴퓨터가 계산하기 더 쉽고 효율적으로 만들기 위한 필수 과정입니다.
- **시각화**: `plt.imshow`를 사용하여 랜덤으로 뽑힌 6개의 이미지를 화면에 띄워 데이터를 확인합니다.

### 2단계: 인공지능 모델 만들기
- **Sequential**: 층을 차례대로 쌓아 모델을 만드는 가장 간단한 방법입니다.
- **Flatten**: 28x28 크기의 2차원 이미지를 784개의 1차원 배열로 펼칩니다. (인공지능이 데이터를 순차적으로 읽게 함)
- **Dense**: 신경망의 기본 단위입니다. 128개의 노드를 가진 층을 통해 특징을 추출합니다.
- **Dropout(0.2)**: **가장 중요한 부분 중 하나**입니다. 학습 중에 무작위로 20%의 데이터를 사용하지 않음으로써, 모델이 특정 데이터에만 과하게 집착하는 '과적합(Overfitting)'을 방지합니다.

### 3단계: EarlyStopping (학습 조기 종료)
- 인공지능 학습은 무작정 오래 한다고 좋은 것이 아닙니다. 
- **EarlyStopping**은 검증 데이터(`val_loss`)를 관찰하다가, 성능 향상이 더 이상 없으면(patience=3, 3번의 기회 부여) 학습을 스스로 멈춥니다. 시간과 자원을 절약해 줍니다.

### 4단계: 학습 및 결과 분석
- **model.fit**: 실제로 모델을 학습시키는 명령어입니다. `validation_split=0.2`는 훈련 데이터의 20%를 중간고사 문제지처럼 사용하여 매번 성적을 체크하게 합니다.
- **차트 출력**: `matplotlib`을 이용해 학습이 진행될수록 오차(Loss)가 줄고 정확도(Accuracy)가 오르는 과정을 그래프로 확인합니다.

### 5단계: 모델 평가
- **model.evaluate**: 학습에 참여하지 않았던 완벽한 '새로운 문제(테스트 셋)'를 모델에게 풀게 합니다. 이를 통해 모델이 실제로 얼마나 똑똑한지 최종 점수를 확인합니다.

---
**💡 팁:** 코드를 실행할 때 `val_loss`가 줄어들지 않으면 Dropout 비율을 조절하거나, Dense 층의 노드 수를 변경해보며 실험해보세요!
